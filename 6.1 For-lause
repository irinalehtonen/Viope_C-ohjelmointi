/* Tee ohjelma, joka kysyy käyttäjältä kokonaisluvun n (n>0) ja tulostaa monitorille luvut 1,2,3..n allekkain. Tee ohjelma hyödyntäen FOR-lausetta. */

#include <stdio.h> 
 
int main() 
{ 
	int i, luku; 
	
	printf("Anna kokonaisluku:");
	scanf("%d", &luku);

		for(i = 1; i <= luku; ++i) { 
		if (i == luku)
			printf("%d\n", i);
		else
			printf("%d\n", i);
	}

	return 0; 
}
